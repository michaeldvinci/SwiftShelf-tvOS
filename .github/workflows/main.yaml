name: apt-test
run-name: apt-test

on: push

jobs:
  apt:
    runs-on: ubuntu-latest
    steps:
      - run: apt install yq -y
  sudo-apt:
    runs-on: ubuntu-latest
    steps:
      - run: sudo apt install yq -y
